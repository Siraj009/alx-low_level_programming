#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alpha[] = "a..z\n";
	
	for (alpha[] = 'a' ; alpha[] <= 'z' ; alpha[]++)
	{
	putchar(alpha[]);

	}

	return (0);
}
